class Solution {
    private:
    int mins(vector<int> s)
    {int min1=0;
        for(auto x:s)
        {
          min1=min(min1,x);
        }
        return min1;
    }
public:
    int findContentChildren(vector<int>& g, vector<int>& s) {
        g.sort(g.begin(),g.end());
        for(int i=0;i<s.size();i++)
        {
            if(mins(g)<=mins(s))
            {

            }
        }

    }
};
